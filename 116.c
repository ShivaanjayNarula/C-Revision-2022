#include <stdio.h>
#include <conio.h>
void main()
{
    int count = 0;
    while(count < 100)
    {
        printf("This is %d\n", count + 1);
        count = count + 1;
    }
    return 0;
}
